# Copy this to .env.local for LOCAL DEVELOPMENT ONLY.
# In production, the React frontend is served by the same FastAPI process,
# so API calls are same-origin and VITE_API_URL should NOT be set.

# Points the frontend at your local FastAPI dev server
VITE_API_URL=http://localhost:8000
